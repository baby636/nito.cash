/**
 * Set Receiving Accounts
 */
const setReceivingAccounts = (state, _accounts) => {
    /* Set receiving accounts. */
    state.receivingAccounts = _accounts
}

/* Export module. */
export default setReceivingAccounts
