/**
 * Set Active Accounts
 */
const setActiveAccounts = (state, _accounts) => {
    /* Set active accounts. */
    state.activeAccounts = _accounts
}

/* Export module. */
export default setActiveAccounts
