/**
 * Set Change Accounts
 */
const setChangeAccounts = (state, _accounts) => {
    /* Set change accounts. */
    state.changeAccounts = _accounts
}

/* Export module. */
export default setChangeAccounts
